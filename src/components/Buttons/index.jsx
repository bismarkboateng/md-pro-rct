

export default function index(props) {
  return (
    <div>
        <button className={props.signInClassName}>Sign in</button>
        <button className={props.signUpClassName}>Sign up</button>
    </div>
  )
}
